# copilotkit-react-fastapi


frontend
```
npm create vite@latest frontend -- --template react-ts
cd frontend && npm install
```


runtime
```
npm install @copilotkit/runtime express dotenv && npm install -D typescript @types/node @types/express tsx
```
